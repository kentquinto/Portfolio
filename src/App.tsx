function App() {
  return (
    <main
      style={{
        height: '100%',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontFamily: 'var(--kq-font-heading)',
      }}
    >
      Portfolio scaffold ready.
    </main>
  );
}

export default App;
